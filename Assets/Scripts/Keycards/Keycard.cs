using UnityEngine;

namespace Keycards
{
    public class Keycard : MonoBehaviour
    {
        // keyId matches the RoomEdge.keyRoomId of the lock  this card opens.
        public string keyId;
    }
}